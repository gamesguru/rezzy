7/25/2026

❯ is m.federate fully checked/supported now?

  Searched for 1 pattern

No — m.federate is not implemented anywhere in the codebase. It's still the open gap flagged in docs/spec_audit.md (Rule 3): rejecting cross-domain event delivery when content.m.federate is false on the create event. It's blocked on the same missing piece as Rules 1.2 and 4 — rezzy has no domain-parsing utility to extract/compare the domain parts of MXIDs and room IDs, so none of those three rules can be checked yet.

✻ Cogitated for 7s

❯ what about rejection tracking? WHat were the other gaps mentioned? summarize it all and let's re-triage

  Read 1 file

Rule 2.3 — rejected-event tracking

Rule 2.3: reject an event if any of its auth_events were themselves rejected. This needs a rejected-event registry — rezzy would have to know, for an arbitrary event ID, "was this rejected by a prior check_auth call?" That's homeserver-side persistent state (a database of past auth decisions across the whole room history), not something a stateless state-resolution library can determine from the DAG slice it's given. It's arguably out of scope for rezzy entirely rather than a "gap to close" — same category as Rule 5.2 (signature verification), which the doc already excludes as HS-side.

Full remaining-gap summary (current state of docs/spec_audit.md)

Blocked on domain parsing (one shared missing utility unlocks all three):
- Rule 1.2 — room_id domain must match sender domain (V1–V11); reject if event has room_id at all
- Rule 3 — m.federate: false should block cross-domain events
- Rule 4 (V1–V7) — m.room.aliases domain mismatch check (also listed Low-priority since it's an ob

auth_events validation (Rule 2.x), needs resolving full auth-event bodies, not just IDs:
- 2.1 — reject duplicate (type, state_key) pairs
- 2.3 — reject if any auth event was itself rejected (homeserver state — likely out of scope, see
- 2.4 — reject if no m.room.create among entries (V1–V11)
- 2.5 — reject if any auth event has wrong room_id
- V12's equivalent: reject if room_id isn't an accepted m.room.create event ID

Soft-checked only:
- 2.2 — auth_events entries must match the selection algorithm; currently just a stderr warning (w never rejects

Low priority / obsolete rule sets:
- Rule 11 (V1–V2) — m.room.redaction: PL ≥ redact level, or same domain (also blocked on domain psince it's V1–V2 only)

Everything else in the audit is [x] fixed — rules 1, 1.3, 1.4, 5.1–5.8 (full member matrix), 6, 7redaction matrix and PDU syntactic invariants tables are fully green except the intentional pre-v11 warn-only row.

For re-triage, the real fork in the road is: 2.1/2.4/2.5/V12-room_id are tractable if check_auth's in resolved auth-event bodies (or IDs it can look up) rather than just the event under test —worth checking what data check_auth actually receives today. 1.2/3/4 all wait on a domain-parsing utility, which is a reasonable single unit of work. 2.3 I'd flag as won't-fix / out-of-scope rather than a backlog item, since it requires state rezzy structurally doesn't have.

Want me to check what check_auth's current signature gives it access to, to see whether 2.1/2.4/2hout new plumbing?


❯ yes


Confirmed and matches the doc's design decision — LeanEvent deliberately has no room_id field, by design (rezzy filters by room_id before calling in). So Rules 2.5 and the V12 room_id rule are structurally out of scope, not just unimplemented — they'd require a field the type intentionally omits.

  Searched for 4 patterns, ran 2 shell commands

Confirmed: warn_unexpected_auth_events is entirely standalone — it's a separate opt-in function callers can invoke themselves, never wired into check_auth's actual Result. That reframes the re-triage significantly.

Good news: 2.1, 2.4, and upgrading 2.2 are all reachable, not blocked. The infrastructure already exists (EventProvider<Id, C> — ID→event lookup, implemented for HashMap) — it's just not plumbed into check_auth. Concretely:
- Rule 2.2 (auth_events entries must match the selection-algorithm types) — the exact logic already exists in warn_unexpected_auth_events; it just needs to become a hard Err inside check_auth instead of a standalone eprintln! helper.
- Rule 2.4 (missing m.room.create among entries, V1–V11) — trivially reachable without even needing EventProvider: state.get_event(M_ROOM_CREATE, "") (already available via StateProvider) gives the create event; just check its ID is in event.auth_events().
- Rule 2.1 (duplicate (type, state_key) pairs) — reachable with EventProvider: resolve each auth_by (type, state_key), reject on collision.

Reframing Rule 2.3 (rejected-ancestor check) — the doc currently says this needs "rejected-event  but check_auth_chain (mod.rs:1300) already accumulates a rejected: Vec<(Id, AuthError)> as it
walks the DAG. So it's not actually impossible — it's a scope question: check_auth itself (single know this, but check_auth_chain (the multi-event driver) already has the exact data needed and
could pass a rejected-ID set down. This is "reachable at the chain level," not homeserver-only.

Still structurally blocked (by design, not by missing plumbing):
- Rule 2.5 and the V12 room_id rule — LeanEvent deliberately omits room_id (explicit doc comment filters by room_id before calling in"). Checking these would mean adding a field the type
intentionally excludes.
- Rules 1.2 / 3 / 4 — still blocked on a domain-parsing utility, unrelated to this plumbing question.

The catch for all of 2.1/2.2/2.4: it requires changing check_auth's signature to accept an option EventProvider<Id, C>>, mirroring how verifier: Option<&dyn EventVerifier<Id>> is already optional— a real but contained API change, and every existing call site (including check_auth_chain, whicwn rejected set through for 2.3) needs updating.

Want me to implement this — add the auth_context parameter, hard-fail rules 2.1/2.2/2.4, and thread the rejected-set through check_auth_chain for 2.3?

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

All rows have 7 columns consistently — CSV is valid. Not staging/committing since the user didn't say "commit."

  Ran 4 shell commands

Docs are updated (staged already, it looks like a hook/linter reformatted the tables for alignment — cosmetic only, content intact). Here's what's left, unchanged from before:

Added:
- docs/spec_coverage.csv: rows for MSC0501 (PinSketch gossip reconciliation, full/pure-CPU-core), the redaction redaction_preserved_keys matrix (full), and PDU syntactic invariants (partial).
- docs/spec_audit.md: new "Redaction Algorithm" table (8 rows, all [x]) and "PDU Syntactic Invariants" table (6 rows, mostly [x], one [~] warn-only, one [ ]).

Still open (nothing new found, same gaps as before, now just properly indexed):

Critical/Medium — auth rules:
- Rule 1.2/2.5/3/4: domain parsing (room_id↔sender match, m.federate, m.room.aliases domain check) — rezzy has no domain-parsing utility at all.
- Rule 1.3: reject unrecognised content.room_version — same gap shows up in the new PDU-invariantsyntactic doesn't check it either.
- Rule 1.4: m.room.create missing creator/invalid additional_creators.
- Rule 2.1/2.3/2.4: auth_events duplicate-pair, rejected-ancestor, and missing-create checks (2.3 g, which is homeserver state rezzy doesn't have).

Low priority:
- Rule 4 (v1–v7 m.room.aliases) and rule 11 (v1–v2 redaction PL/domain check) — both obsolete rule

MSC0501-specific (from TODO.txt, not a correctness gap):
- Two un-squashed fixup commits pending rebase before merge.
- Homeserver HTTP route integration (/_matrix/federation/v1/reconcile/...) is explicitly out of se.

No new spec violations turned up beyond what was already tracked — this was a coverage gap in thein the code.
  ❯ src/reconcile/0501-federation-room-gossip-reconciliation.md 132ms (unchanged)
    markdownlint $(git ls-files '*.md')
  ❯ src/reconcile/0501-federation-room-gossip-reconciliation.md 132ms (unchanged)
    markdownlint $(git ls-files '*.md')
  ❯ src/reconcile/0501-federation-room-gossip-reconciliation.md 132ms (unchanged)

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Good — that confirms it. docs/spec_coverage.csv and docs/spec_audit.md haven't been touched since commit 3caa4ca, but a lot has landed since then that they don't reflect:

Not tracked in the docs at all:
- MSC0501/MSC45xx PDU set reconciliation via PinSketch (0c7cca2) — a whole new gossip-reconciliation subsystem, no CSV row.
- Redaction rules matrix (redaction_preserved_keys across room versions 1–11+, ab60e5a/earlier commits) — spec_audit.md only mentions redaction in the auth-rule context (rule 11, obsolete), not the actual event-redaction key-preservation matrix.
- PDU syntactic invariants (d6fd27b, 9ce24c1) — $-prefix event ID rules, MXID localpart charset validation, version-gated 255-byte length limits, depth bounds via MAX_SAFE_JSON_INTEGER. None of this maps to a spec_audit.md row; it's closest to audit rule 1.3 ("reject unrecognised content.room_version" — still [ ]) and arguably new ground the audit doesn't have categories for (PDU-level syntax vs. auth rules).
- event_hash trait method for non-string ID backends (9f64774) — internal, probably doesn't need a spec row.
- LtHash loop optimization / streaming cycle-error handling (ab60e5a) — implementation detail, not spec-relevant, fine to leave untracked.

No new violations beyond what's already flagged — the audit's existing gap list (rules 1.2–1.4 domain/room_version/creator checks, 2.x auth_events validation, rule 3 m.federate, rule 4 aliases, rule 11 v1–v2 redaction-PL) is still accurate for what it covers. The issue is coverage, not correctness: the two tracking files are about 5 feature commits stale and missing entries for two entire subsystems (PinSketch reconciliation, redaction matrix) plus the PDU syntactic-validation work from this session.

Want me to update docs/spec_coverage.csv and docs/spec_audit.md to add rows for MSC0501, the redaction matrix, and the PDU syntactic checks?

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

  Here is the status of the repository and the current branch (feat/msc45xx-pdu-set-reconciliation-via-pinsketch).
  ──────
  ### 1. Current Branch: feat/msc45xx-pdu-set-reconciliation-via-pinsketch

  The implementation of MSC0501 / MSC45xx PDU Set Reconciliation via PinSketch is feature-complete in terms of logic, tests, and benchmarks.

  #### What was done:

  • Syndrome Decoding & GF(2⁶⁴) Math: Implemented pinsketch polynomial root factorization over GF (2⁶⁴), 128-bit integrity accumulators, and wire serialization (algebraic.rs, gf64.rs).
  • Client & Server Protocol Pipelines: Built client.rs and server.rs for bucket summary sketches and pre-round gating.
  • Triage & Resident Storage: Strata-based resident storage (resident.rs) and bucket triage decoding (triage.rs).
  • Verification & Lints: 100% test pass rate (cargo test) and zero Clippy warnings (cargo clippy --all-targets --all-features).

  #### What's left on this branch:

  1. Commit Cleanup: There are two un-squashed fixup! commits on HEAD (c3f557e & cb4c7ca targeting acfdce7) that should be squashed via git rebase -i --autosquash before final merge into master / dev.
  2. Homeserver HTTP Handler Integration: rezzy provides the synchronous pure-CPU core routines (ReconciliationClient, build_bucket_sketches, decode_bucket_sketches). Homeserver integrations (e.g., Conduwuit/Synapse) need to
  map these to Matrix federation HTTP routes (/_matrix/federation/v1/reconcile/...).
  ──────
  ### 2. General Codebase TODOs & Open Items

  A codebase search reveals a few pending technical debt items / TODOs in other modules:

  • State Resolution / Graph Traversal (at.rs):
      • Evaluate V2.2 (MSC4242 State DAGs) requirements for merge base selection.
      ✓ Handle CycleDetected errors explicitly via return status in streaming optimization rather than swallowing in logs.
  • LtHash Allocation (lthash.rs):
      ✓ Optimize LtHash SIMD loop unrolling (8-lane chunks), pass references for seeds, and single-pass checksum byte updating.
  • Spec Invariants & Redaction (rezzy_types.rs):
      ✓ Enforce PDU structural syntax validation ($ prefix rules for event IDs, MXID sender validation, depth bounds).
      ✓ Expand test coverage for soft-failed vs. rejected events.
      ✓ Complete full redaction rules matrix (`redaction_preserved_keys`) across Matrix room versions 1–11+.

  ──────
  ### 3. Other Active / Parallel Branches in Repository

  • fix/anomaly-17-synthetic-fixture: Replaces a heavy 4,503-line DAG test file with a lightweight 14-event synthetic fixture.
  • chore/benchmark-profiling: Benchmarking branch comparing iterative sorting against lattice folding.
  • origin/feat/power-level-spec-compliance: Fixes version awareness during auth checks for 3rd-party invites.



~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

7/15/2026

› should we define a new folder like biz or etc or lib or something to represent not state rez related stuff, but still outofband related?


• Yes. I would not put this under state, resolve, or auth; it is not state resolution logic.

  I’d use a domain name, not a generic bucket:

  src/out_of_band/
    mod.rs
    cuckoo_verify.rs
    server_keys.rs   // later, if needed
    minting.rs       // later, if this grows

  Then in src/lib.rs:

  pub mod out_of_band;

  And callers use:

  rezzy::out_of_band::cuckoo_verify::verify_minting_pow(...)

  I’d avoid names like biz, etc, misc, or lib. They become junk drawers quickly. out_of_band is explicit enough: this is adjacent Matrix machinery, but not state resolution.

  If you expect more PQ/server-key work, another defensible option is:

  src/federation/
    mod.rs
    server_keys/
      mod.rs
      cuckoo_verify.rs

  But for this repo, out_of_band is probably the better boundary because it says “not core state res” without overcommitting to a full federation stack.


› does this belong in its own micro-service backend?


• Probably yes, if this is intended for real key minting.

  For verification only, keeping it as a library is fine. Verification is cheap, deterministic, and useful inside homeservers/notaries.

  For minting/solving, I’d separate it:

  rezzy crate:
    verify embedded PoW
    derive key_id / short_key_id
    validate key object shape

  minting service:
    generate FN-DSA keypair
    search Cuckoo nonce/solution
    emit server key object
    manage CPU/GPU workers

  Reasons to split minting:

  - It is expensive and operationally different from request handling.
  - It may need GPU/large-memory workers or native solver binaries.
  - It has different failure modes, queues, progress reporting, cancellation, and rate controls.
  - It touches private key generation/signing, which benefits from tighter isolation.
  - Homeservers and notaries need verification, but usually do not need to mint often.

  I would not make verification a microservice. That adds latency and another dependency to a hot path. Keep verification as an in-process Rust library.

  So the clean split is: library verifier in rezzy or a small shared crate; separate minting backend/service for creating keys and PoW stamps.
