[package]
name = "ruma-lean"
version = "0.1.4"
edition = "2021"
authors = ["Shane Jaroch <chown_tee@proton.me>"]
description = "Formally verified, dependency-free Matrix State Resolution v2 logic."
license = "MIT OR Apache-2.0"
repository = "https://github.com/gamesguru/ruma-lean"
autotests = false

[[bin]]
name = "ruma-lean"
path = "src/main.rs"
required-features = ["cli"]

[features]
default = ["std"]
std = ["alloc", "serde_json/std", "serde/std"]
alloc = []
zkvm = []
cli = ["std", "dep:clap", "dep:anyhow", "dep:ureq"]

[dependencies]
anyhow = { version = "1.0", optional = true }
clap = { version = "4.4", optional = true, features = ["derive", "env"] }

hashbrown = { version = "0.15", default-features = false, features = ["default-hasher", "inline-more"] }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }
ureq = { version = "2.9", optional = true, features = ["json"] }

[dev-dependencies]
[[test]]
name = "ruma_upstream"
path = "tests/ruma_upstream.rs"
[[test]]
name = "snapshots"
path = "tests/snapshots.rs"
[[test]]
name = "sanity"
path = "tests/sanity.rs"

exclude = [
    "res/",
    "scripts/",
    "docs/",
    "RumaLean/",
    "RumaLean.lean",
    "lake*",
    "lean-toolchain",
    ".envrc",
    ".env.example",
    ".github",
    ".gitignore",
    ".editorconfig",
    ".pre-commit-config.yaml",
    ".prettierignore",
    ".prettierrc",
    ".rgignore",
    "tests/",
    "Makefile",
]
